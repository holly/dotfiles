---
description: Commit changes with conventional commit format
---

以下の手順で git commit を実行する:

1. `git status` と `git diff` で変更を確認
2. `git log --oneline -10` で最近のコミット履歴を確認
3. git-commit skill に従って commit メッセージを生成
4. 変更ファイルを確認し、ユーザーに staging してよいか確認
5. 適切なファイルを `git add` して `git commit`
